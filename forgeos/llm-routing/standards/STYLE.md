# Style Guide

Use clear contract-first language.

Naming:

- use lowercase ids
- use snake_case for database fields
- use kebab-case for file names
- use provider_model_id for native provider names
- use model_id for normalized ForgeOS ids

Documentation:

- every limit needs a source
- every provider fact needs a timestamp
- every route needs a reason
- every fallback needs a chain
