# Deprecated: renamed to forge-pi-kit

This ForgeOS wrapper pack was renamed to avoid colliding with the existing `oh-my-pi` wrapper project.

Use this path instead:

```txt
forgeos/wrappers/forge-pi-kit/
```

Start here:

```bash
cat forgeos/wrappers/forge-pi-kit/QUICKSTART.md
cat forgeos/wrappers/forge-pi-kit/README.md
cat forgeos/wrappers/forge-pi-kit/profiles/runpod-vllm.yaml
```

The active enforcement layer remains:

```txt
forgeos/wrappers/pi-contract-wrapper/
```

Do not add new ForgeOS files under `forgeos/wrappers/oh-my-pi/`.
